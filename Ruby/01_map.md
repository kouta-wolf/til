## 配列.map{|変数|処理}の使い方
- 渡された配列をブロック内で処理を繰り返して = の左辺の変数に返す
- gets.splitの時点で配列であるため、構文を1行で書くことが可能
- each doと処理が似通っているが、配列を新しく作るのが趣旨のため分かりやすさが上らしい

## 配列.map(&:メソッド)の使い方
- 配列にメソッドを適用させることができる。to_iやup,downcase等
- ブロック（上の変数処理）を渡す代わりに使っているため併用することはできないことに注意